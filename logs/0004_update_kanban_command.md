# 0004_update_kanban_command 作業ログ

## 基本情報

- **タスクファイル**: kanban/0004_update_kanban_command.md
- **開始日時**: 2026-04-15T13:49:32+09:00
- **完了日時**: 2026-04-15T13:51:37+09:00

## タスク概要

`/kanban` コマンドで読み込むマークダウンファイルにおいて、目的に該当する項目がなければリジェクトし、プランまで進まずに終了するようにしてほしい。

## 調査結果

### `.claude/commands/kanban.md`（全60行）

- `/kanban` コマンドの実装本体
- フェーズ1（プランニング）とフェーズ2（実装）の2フェーズ構成
- フェーズ1の手順:
  1. `kanban/` から対象タスクファイルを読み込み、タスク内容を理解する
  2. コードベースを調査し、実装方針を検討する
  3. 計画をまとめ、kanban ファイルの `## プラン` セクションに記載する
  4. ExitPlanMode でユーザーの承認を待つ
  5. リジェクト時のサイクル処理
- 引数なしの場合: `kanban/` 内の未完了タスク（`## 完了サマリー` を含まないファイル）のうち番号が最大のものを自動選択
- 現在のフェーズ1の手順1で「タスク内容を理解する」とあるが、目的（Why）の存在確認は明示されていない
- バリデーション失敗時の終了処理は存在しない

### `.claude/kanban-workflow.md`（全157行）

- kanban ワークフローの詳細手順書・テンプレート集
- kanban ファイルへの追記テンプレート構造:
  ```markdown
  ## プラン
  （内容）
  ---
  ## 完了サマリー
  （内容）
  ```
- kanban ファイルの基本構造テンプレートは明示されていない（`## 要望` が基本構造であることは暗黙的）
- ログファイルテンプレートは `## 基本情報`、`## タスク概要`、`## 調査結果`、`## 実装プラン`、`## プランニング経緯`、`## 会話内容`、`## 編集したファイル`、`## 実行したコマンド`、`## 判断・意思決定`、`## エラー・問題` の構造
- タスク検出ロジック: `## 完了サマリー` を含まないファイルが未完了タスク

### 既存 kanban ファイルの構造分析

- `0000_sync_agents_md.md`: `# タイトル` + `## 要望` + `## プラン` + `## 完了サマリー`（完了済み）
- `0001_update_kanban_command.md`: `# タイトル` + `## 要望` + `## プラン` + `## 完了サマリー`（完了済み）
- `0002_rename.md`: `# タイトル` + `## 要望` + `## プラン` + `## 完了サマリー`（完了済み）
- `0003_update_kanban_command.md`: `# タイトル` + `## 要望` + `## プラン` + `## 完了サマリー`（完了済み）
- `0004_update_kanban_command.md`: `# タイトル` + `## 要望` のみ（未完了・本タスク）

全ての既存ファイルに `## 目的`（Why）セクションは存在しない。

### ユーザーとの対話から得た要件

- `## 要望` は What/How（どのような機能にしたいか）
- `## 目的` は Why（なぜこの作業が必要か）— 背景・動機・理由
- セクション名は固定しない。Claude が内容を読んで「Why」に該当するか判断する
- 将来的に kanban の蓄積から設計出力に活用する狙いがある

## 実装プラン

### 変更対象
- `.claude/commands/kanban.md`
- `.claude/kanban-workflow.md`

### `.claude/commands/kanban.md` の変更

フェーズ1の手順1と2の間に、目的セクションのバリデーションステップを挿入する。

変更前の手順:
```
1. `kanban/` から対象タスクファイルを読み込み、タスク内容を理解する
2. コードベースを調査し、実装方針を検討する
```

変更後の手順:
```
1. `kanban/` から対象タスクファイルを読み込み、タスク内容を理解する
2. タスクファイルに「目的」（Why: なぜこの作業が必要か）に該当するセクションがあるか確認する
   - `## 要望` は What/How に該当するため、目的（Why）とはみなさない
   - セクション名は問わない（`## 目的`、`## 背景` など）。内容を読み、作業の動機・背景・理由が記載されているか判断する
   - 目的に該当するセクションが**ない場合**: ユーザーに「目的（Why）に該当するセクションが見つかりません。kanban ファイルに目的セクションを追加してください。」と報告し、プランモードに入らず終了する
3. コードベースを調査し、実装方針を検討する
（以降は繰り下げ）
```

### `.claude/kanban-workflow.md` の変更

kanban ファイルの基本構造テンプレートを追記する。`## kanban ファイルへの追記テンプレート` セクションの前に、kanban ファイルの基本構造を示す新セクションを追加する。

```markdown
## kanban ファイルの基本構造

kanban ファイルはユーザーが以下の構造で作成する。`## 目的` セクションが必須。

```markdown
# タイトル
## 目的
（なぜこの作業が必要か — 背景・動機・ゴール）

## 要望
（具体的に何をどうしてほしいか）
```

### 代替案と却下理由

**代替案A: セクション名を `## 目的` に固定する**  
却下理由: ユーザーが「セクション名は固定しない」と明示。柔軟性を保つべき。

**代替案B: check-kanban.sh などのフックスクリプトでバリデーションを行う**  
却下理由: フックは現状 `PostToolUse` で Edit/Write 後に実行されるもの。`/kanban` コマンド実行時の入力バリデーションには Claude の判断が必要で、シェルスクリプトでは意味的な判定が難しい。

**採用アプローチ: kanban コマンドの指示文にバリデーションロジックを追加**  
理由: Claude に意味的な判断をさせることで、セクション名に依存しない柔軟なバリデーションが実現できる。

## プランニング経緯

### 初回提案

フェーズ1の手順にバリデーションステップを追加し、「目的（Why）」セクションの存在確認を行う。セクション名は固定せず意味的に判断。なければ終了。

### ユーザーフィードバック

初回提案がそのまま承認された。ただし、AskUserQuestion でユーザーに確認した内容:
- 「目的に該当する項目」= `## 要望`（How）とは別の「Why」セクション
- セクション名は未定・固定しない、Claude が判断する
- 将来的に kanban 蓄積からの設計出力活用を視野に入れている

### 最終プラン

変更なし（初回提案をそのまま採用）

## 会話内容

### [13:30頃] ユーザー指示

`/kanban` コマンドで kanban ファイルに目的に該当する項目がなければリジェクトし、プランまで進まずに終了するようにしてほしい（0004タスク）。

### [13:35頃] Claude による確認（AskUserQuestion）

「目的に該当する項目」が何を指すか確認。選択肢: `## 要望` セクション / `## 要望` + `# タイトル` / 未完了タスクの存在確認。

ユーザー回答: 「今回の作業の目的です。要望セクションなどとは別に目的に該当するセクションの存在を確認して欲しい」

### [13:40頃] Claude による確認（AskUserQuestion）

`## 目的` セクションの位置と内容を確認しようとしたが、ユーザーが質問を拒否。

### [13:42頃] ユーザーによる説明

「目的」は Why（なぜ作業が必要か）、「要望」は How（どんな機能にしたいか）。セクション名は固定しない。Claude が読んで判断してほしい。将来的に kanban 蓄積から設計出力を考えている。

### [13:45頃] プラン提示 → 承認

上記プランをユーザーに提示し、承認を得た。

## 編集したファイル

| ファイル | 変更内容 |
|---------|---------|
| `.claude/commands/kanban.md` | フェーズ1にバリデーションステップ（手順2）を追加 |
| `.claude/kanban-workflow.md` | kanban ファイルの基本構造テンプレートを追加 |

## 実行したコマンド

```bash
TZ=Asia/Tokyo date +"%Y-%m-%dT%H:%M:%S+09:00"
```

## 判断・意思決定

- バリデーションはシェルスクリプトではなく Claude の意味的判断で行う（セクション名固定は避ける）
- 既存の完了済み kanban ファイルには `## 目的` がないが、過去分の遡及修正は行わない（新規タスクから適用）

## エラー・問題

（なし）

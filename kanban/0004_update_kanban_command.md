# `/kanban` コマンドを更新する
## 要望
`/kanban` コマンドで読み込むマークダウンファイルにおいて、目的に該当する項目がなければリジェクトし、プランまで進まずに終了するようにしてほしい。

## プラン

- `.claude/commands/kanban.md` のフェーズ1にバリデーションステップ（手順2）を追加
  - タスクファイルに「目的」（Why）セクションがあるか確認
  - `## 要望` は How とみなし、目的（Why）とは区別する
  - セクション名は固定しない（Claude が意味的に判断）
  - 目的セクションがなければ、プランモードに入らず終了
- `.claude/kanban-workflow.md` に kanban ファイルの基本構造テンプレートを追加
  - `## 目的`（Why）が必須項目であることを明記

---

## 完了サマリー

- **完了日時**: 2026-04-15T13:51:37+09:00
- **対応内容**:
  - `.claude/commands/kanban.md`: フェーズ1の手順2としてバリデーションステップを追加（目的セクション不在時はプランモードに入らず終了）
  - `.claude/kanban-workflow.md`: 「kanban ファイルの基本構造」セクションを追加（`## 目的` が必須であることを明記）
- **変更ファイル**:
  - `.claude/commands/kanban.md`
  - `.claude/kanban-workflow.md`